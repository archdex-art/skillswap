{
  "name": "skillswap",
  "private": true,
  "version": "1.0.0",
  "description": "SkillSwap monorepo root",
  "scripts": {
    "dev": "concurrently -k \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}